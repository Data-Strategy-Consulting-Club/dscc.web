<div id="<%= section %>_footer" class="pt-2 flex items-center justify-between">
  <span class="text-[11px] sm:text-xs text-gray-500">
    <%= records.count %> <%= "image".pluralize(records.count) %>
  </span>
  <%= link_to "+ Add Image", add_image_admin_content_path(section: section),
      data: { turbo_method: :post },
      class: "text-xs sm:text-sm text-blue-600 hover:text-blue-800 font-medium cursor-pointer" %>
</div>

